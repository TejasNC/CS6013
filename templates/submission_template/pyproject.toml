[project]
name = "cs6013-submission"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "torch==2.12.1",
    "transformers==5.16.1",
    "accelerate==1.14.0",
    "huggingface-hub==1.29.0",
    # Add technique-specific deps here, e.g.:
    # "gptqmodel==7.3.2",
    # "optimum==2.3.0",
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
